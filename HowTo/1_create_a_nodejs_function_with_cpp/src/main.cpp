#include "main.h"

#include <node_api.h>


// a Node.js function written in C++.
// The function should follow the signature of a napi_callback:
// napi_value (*napi_callback)(napi_env, napi_callback_info);

napi_value mainf(napi_env env, napi_callback_info info) {
    return NULL;
}